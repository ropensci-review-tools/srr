# CRAN notes for srr_1.0.0 submission

This is an initial submission. Note that all exported functions have examples, but many lines are wrapped in either `dontrun` or `donttest`, because they download external files.

## Test environments

The package has been checked on all environments listed below, and generates two notes:

- identifying the package as a new submission.
- Potentially mis-spelled "rOpenSci" and "Roclets", but both are correct.

GitHub actions:
* Linux: R-release, R-devel
* OSX: R-release
* Windows: R-release

CRAN win-builder:
* R-oldrelease, R-release, R-devel
